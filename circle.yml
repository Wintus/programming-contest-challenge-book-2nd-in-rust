machine:
  services:
    - docker

dependencies:
  pre:
    - |
      if [ ! -x cargo ]; then
        curl -sf -L https://static.rust-lang.org/rustup.sh | sh /dev/stdin --channel=nightly --yes
      fi
  override:
    - cargo build

test:
  override:
    - cargo test
